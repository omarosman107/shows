$("#abcModal").on('hidden.bs.modal',function(e){$("#abcModal iframe").attr("src",$("#abcModal iframe").attr("src"));});
$("#rtModal").on('hidden.bs.modal',function(e){$("#rtModal iframe").attr("src",$("#rtModal iframe").attr("src"));});
$("#weatherModal").on('hidden.bs.modal',function(e){$("#weatherModal iframe").attr("src",$("#weatherModal iframe").attr("src"));});
$("#vevoModal").on('hidden.bs.modal',function(e){$("#vevoModal iframe").attr("src",$("#vevoModal iframe").attr("src"));});
$("#bloomModal").on('hidden.bs.modal',function(e){$("#bloomModal iframe").attr("src",$("#bloomModal iframe").attr("src"));});
$("#sntvModal").on('hidden.bs.modal',function(e){$("#sntvModal iframe").attr("src",$("#sntvModal iframe").attr("src"));});
$("#hctvModal").on('hidden.bs.modal',function(e){$("#hctvModal iframe").attr("src",$("#hctvModal iframe").attr("src"));});
$("#wilxModal").on('hidden.bs.modal',function(e){$("#wilxModal iframe").attr("src",$("#wilxModal iframe").attr("src"));});
$("#espnModal").on('hidden.bs.modal',function(e){$("#espnModal iframe").attr("src",$("#espnModal iframe").attr("src"));});
$("#universalModal").on('hidden.bs.modal',function(e){$("#universalModal iframe").attr("src",$("#universalModal iframe").attr("src"));});
$("#aljazeeraModal").on('hidden.bs.modal',function(e){$("#aljazeeraModal iframe").attr("src",$("#aljazeeraModal iframe").attr("src"));});
$("#cableModal").on('hidden.bs.modal',function(e){$("#cableModal iframe").attr("src",$("#cableModal iframe").attr("src"));});
$("#cbsnModal").on('hidden.bs.modal',function(e){$("#cbsnModal iframe").attr("src",$("#cbsnModal iframe").attr("src"));});
$("#cnnModal").on('hidden.bs.modal',function(e){$("#cnnModal iframe").attr("src",$("#cnnModal iframe").attr("src"));});
$("#cartoonNModal").on('hidden.bs.modal',function(e){$("#cartoonNModal iframe").attr("src",$("#cartoonNModal iframe").attr("src"));});
$("#espnModal").on('hidden.bs.modal',function(e){$("#espnModal iframe").attr("src",$("#espnModal iframe").attr("src"));});
$("#nflModal").on('hidden.bs.modal',function(e){$("#nflModal iframe").attr("src",$("#nflModal iframe").attr("src"));});
$("#nickModal").on('hidden.bs.modal',function(e){$("#nickModal iframe").attr("src",$("#nickModal iframe").attr("src"));});
$(document).ready(function(){(function($){$('#filter').keyup(function(){var rex=new RegExp($(this).val(),'i');$('.searchable tr').hide();$('.searchable tr').filter(function(){return rex.test($(this).text());}).show();})}(jQuery));});
